spring:
  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:DB/migration
  datasource:
    url: jdbc:mysql://mysql:3306/vollmed_api_test?createDatabaseIfNotExist=true&autoReconnect=true&allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: 12345678
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      show-sql: true
server:
  port: 9001
